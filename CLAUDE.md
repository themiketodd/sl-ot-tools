# sl-ot-tools

## Version

**Always bump the version in `src/sl_ot_tools/__init__.py` before pushing.** Use semver:
- Patch (0.2.x) for bug fixes
- Minor (0.x.0) for new features
- Major (x.0.0) for breaking changes
