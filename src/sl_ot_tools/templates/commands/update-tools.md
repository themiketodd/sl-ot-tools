Update sl-ot-tools to the latest version and refresh skills.

1. Run: `uv pip install -e ~/code/sl-ot-tools --system --break-system-packages`
2. If the install fails, show the error and stop
3. Run: `sl-ot-tools setup`
4. Confirm that the update completed successfully
