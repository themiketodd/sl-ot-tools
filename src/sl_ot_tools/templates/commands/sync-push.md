Push locally-created files to SharePoint.

1. Run: `{{ENGAGEMENT_DIR}}/sync_to_sharepoint.sh --dry-run`
2. Show me a summary of what would be pushed (new or updated files, target folders)
3. Ask if I want to proceed
4. If yes, run: `{{ENGAGEMENT_DIR}}/sync_to_sharepoint.sh`
5. Confirm that OneDrive will sync the changes to SharePoint
